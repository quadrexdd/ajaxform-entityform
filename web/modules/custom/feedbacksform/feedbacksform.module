<?php

/**
 * Implements hook_theme().
 * returns rows variable in order to render it in TWIG
 */
function feedbacksform_theme($existing, $type, $theme, $path) {
  return [
    'feedbacks_template' => [
      'variables' => ['rows' => 'rows'],
    ],
  ];
}
